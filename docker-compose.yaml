version: '3'

services:
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - 8080:80
  #   volumes:
  #     - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
  #   depends_on:
  #     - app

  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
    - "3000:3000"